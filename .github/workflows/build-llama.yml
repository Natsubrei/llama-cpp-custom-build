name: Build llama.cpp CUDA All Quants

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-2022

    steps:
      - name: Checkout llama.cpp
        uses: actions/checkout@v6
        with:
          repository: ggml-org/llama.cpp
          path: llama-src
          fetch-depth: 1

      - name: Install CUDA 13.3
        uses: ./llama-src/.github/actions/windows-setup-cuda
        with:
          cuda_version: "13.3"
          cuda_arch: "x64"

      - name: Install Ninja
        shell: powershell
        run: choco install ninja -y

      - name: Configure
        shell: cmd
        run: |
          call "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvarsall.bat" x64

          cmake -S llama-src -B build -G "Ninja Multi-Config" ^
            -DGGML_CUDA=ON ^
            -DGGML_CUDA_FA=ON ^
            -DGGML_CUDA_FA_ALL_QUANTS=ON ^
            -DGGML_NATIVE=OFF ^
            -DCMAKE_CUDA_ARCHITECTURES=120a-real ^
            -DLLAMA_BUILD_SERVER=ON ^
            -DLLAMA_BUILD_TESTS=OFF ^
            -DLLAMA_BUILD_EXAMPLES=OFF

      - name: Build llama-server
        shell: cmd
        run: |
          call "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvarsall.bat" x64
          cmake --build build --config Release --target llama-server -j %NUMBER_OF_PROCESSORS%

      - name: Copy CUDA runtime DLLs
        shell: powershell
        run: |
          $dst = "build\bin\Release"

          Get-ChildItem "$env:CUDA_PATH\bin" -Filter "cudart64_*.dll" |
            Copy-Item -Destination $dst

          Get-ChildItem "$env:CUDA_PATH\bin" -Filter "cublas64_*.dll" |
            Copy-Item -Destination $dst

          Get-ChildItem "$env:CUDA_PATH\bin" -Filter "cublasLt64_*.dll" |
            Copy-Item -Destination $dst

      - name: Verify build
        shell: powershell
        run: |
          .\build\bin\Release\llama-server.exe --version

          Write-Host ""
          Write-Host "===== CUDA build configuration ====="
          Select-String `
            -Path "build\CMakeCache.txt" `
            -Pattern "GGML_CUDA|GGML_CUDA_FA|CMAKE_CUDA_ARCHITECTURES"

      - name: Package
        shell: powershell
        run: |
          New-Item -ItemType Directory -Force -Path package | Out-Null

          Copy-Item "build\bin\Release\*" "package\" -Recurse

          Compress-Archive `
            -Path "package\*" `
            -DestinationPath "llama-cpp-win-cuda13.3-blackwell-all-quants.zip"

      - name: Upload
        uses: actions/upload-artifact@v6
        with:
          name: llama-cpp-win-cuda13.3-blackwell-all-quants
          path: llama-cpp-win-cuda13.3-blackwell-all-quants.zip
